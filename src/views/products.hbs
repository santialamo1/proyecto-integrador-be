<h1>Products</h1>
{{#each products.docs}}
    <div>
        <h2>{{this.name}}</h2>
        <p>{{this.description}}</p>
        <p>{{this.price}}</p>
    </div>
{{/each}}
<form action="/api/auth/logout" method="POST">
    <button type="submit">Logout</button>
</form>